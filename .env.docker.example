# Cinephage Docker Compose Environment Variables
# Copy this file to .env and configure your settings for docker-compose

# =============================================================================
# DOCKER CONTAINER CONFIGURATION
# =============================================================================

# Container name (default: cinephage)
# CINEPHAGE_CONTAINER_NAME=cinephage

# Restart policy (default: unless-stopped)
# Options: no, always, on-failure, unless-stopped
# CINEPHAGE_RESTART_POLICY=unless-stopped

# Port to expose on the host (default: 3000)
# The container always uses port 3000 internally
# CINEPHAGE_PORT=3000

# =============================================================================
# VOLUME PATHS
# =============================================================================

# Path to store database and settings (default: ./data)
# CINEPHAGE_DATA_PATH=./data

# Path to store log files (default: ./logs)
# CINEPHAGE_LOGS_PATH=./logs

# Path to your media library (default: /path/to/your/media)
# IMPORTANT: Change this to point to your actual media folder
# CINEPHAGE_MEDIA_PATH=/path/to/your/media

# =============================================================================
# USER/GROUP CONFIGURATION
# =============================================================================

# User ID - set to match your host user (run 'id -u' to find yours)
# CINEPHAGE_PUID=1000

# Group ID - set to match your host user (run 'id -g' to find yours)
# CINEPHAGE_PGID=1000

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# REQUIRED: Set this to your server's access URL for CSRF protection
# Examples: http://localhost:3000, https://cinephage.yourdomain.com
# CINEPHAGE_ORIGIN=http://localhost:3000

# Timezone (default: UTC)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# CINEPHAGE_TZ=UTC

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Enable/disable file logging (default: true)
# CINEPHAGE_LOG_TO_FILE=true

# =============================================================================
# BROWSER SOLVER (Advanced)
# =============================================================================

# Enable browser solver for Cloudflare bypass (default: false)
# NOTE: Requires Chromium, which is not included in the default image
# CINEPHAGE_BROWSER_SOLVER_ENABLED=false

# =============================================================================
# WORKER CONFIGURATION (Advanced)
# =============================================================================

# Maximum concurrent workers by type
# Adjust these based on your system resources

# Maximum concurrent stream workers (default: 10)
# CINEPHAGE_WORKER_MAX_STREAMS=10

# Maximum concurrent import workers (default: 5)
# CINEPHAGE_WORKER_MAX_IMPORTS=5

# Maximum concurrent scan workers (default: 2)
# CINEPHAGE_WORKER_MAX_SCANS=2
